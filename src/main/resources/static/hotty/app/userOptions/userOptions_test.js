'use strict';

describe('myApp.userOptions module', function() {

  beforeEach(module('myApp.userOptions'));

  describe('userOptions controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var userOptionsCtrl = $controller('UserOptionsCtrl');
      expect(userOptionsCtrl).toBeDefined();
    }));

  });
});