'use strict';

describe('myApp.mainEditor module', function() {

  beforeEach(module('myApp.mainEditor'));

  describe('mainEditor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainEditorCtrl = $controller('MainEditorCtrl');
      expect(mainEditorCtrl).toBeDefined();
    }));

  });
});